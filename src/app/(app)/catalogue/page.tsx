import { PlaceholderPage } from "@/components/shared/placeholder-page";

export default function CataloguePage() {
  return (
    <PlaceholderPage
      phase={5}
      description="Browse parts, brands, categories, models, and vehicle compatibility."
    />
  );
}
