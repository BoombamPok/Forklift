import { PlaceholderPage } from "@/components/shared/placeholder-page";

export default function InventoryPage() {
  return (
    <PlaceholderPage
      phase={3}
      description="Search, filter, add, edit, and track physical stock with full movement history."
    />
  );
}
