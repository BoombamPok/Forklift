import { PlaceholderPage } from "@/components/shared/placeholder-page";

export default function WarehouseRackDetailPage() {
  return (
    <PlaceholderPage
      phase={4}
      description="Rack detail: its shelves, boxes, and what's stored in each."
    />
  );
}
