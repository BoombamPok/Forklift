import { PlaceholderPage } from "@/components/shared/placeholder-page";

export default function CataloguePartsPage() {
  return (
    <PlaceholderPage
      phase={5}
      description="Master catalogue of parts, part numbers, OEM references, and cross-references."
    />
  );
}
