import { PlaceholderPage } from "@/components/shared/placeholder-page";

export default function CatalogueModelDetailPage() {
  return (
    <PlaceholderPage
      phase={5}
      description="Model detail and the parts known to be compatible with it."
    />
  );
}
