import { PlaceholderPage } from "@/components/shared/placeholder-page";

export default function CatalogueModelsPage() {
  return (
    <PlaceholderPage
      phase={5}
      description="Forklift models, model families, and their part compatibility."
    />
  );
}
